/**
 * InlineChart stub — chart features removed from Agent Farm.
 */
import type { ChartData } from './extractCharts';

export default function InlineChart({ chart }: { chart: ChartData }) {
  return null;
}
